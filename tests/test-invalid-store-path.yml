name: Test OpenFGA action fails with invalid store file path
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Run OpenFGA model test action with non-existent store file
        id: action
        uses: .
        with:
          store-file-path: /nonexistent/path/store.fga.yaml
        continue-on-error: true
      - name: Assert action failed as expected
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "action outcome: $OUTCOME"
          if [ "$OUTCOME" != "failure" ]; then
            echo "FAIL: expected action to fail with invalid store file path, but got outcome: $OUTCOME"
            exit 1
          fi
          echo "PASS: action correctly failed with invalid store file path"
